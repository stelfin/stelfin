{
  "name": "stelfin-web",
  "private": true,
  "description": "Dev-time TypeScript source for the landing page. Compiled output is committed under static/ and is what actually ships — this is not part of the Docker build.",
  "scripts": {
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
